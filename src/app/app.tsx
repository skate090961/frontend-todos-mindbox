import { TodosPage } from '@/pages/todos';
import { Container } from '@radix-ui/themes';

const App = () => {
    return (
        <Container>
            <TodosPage />
        </Container>
    );
};

export default App;
